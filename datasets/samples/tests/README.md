# Tests for datasets/samples

This directory holds tests, fixtures, validation cases, or executable checks owned by `datasets/samples`. Add real tests here as the area becomes executable; do not use this directory as evidence of implementation by itself.
