# Tests for evaluations/components/code-quality-verifier

This directory holds tests, fixtures, validation cases, or executable checks owned by `evaluations/components/code-quality-verifier`. Add real tests here as the area becomes executable; do not use this directory as evidence of implementation by itself.
