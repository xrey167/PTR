# code-quality-verifier

PTR contract and candidate comparison for **code-quality-verifier**. Add benchmarks under `evidence/` and update `candidates.toml`; keep the architecture-facing trait stable.
