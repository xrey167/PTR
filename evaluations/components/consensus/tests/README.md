# Tests for evaluations/components/consensus

This directory holds tests, fixtures, validation cases, or executable checks owned by `evaluations/components/consensus`. Add real tests here as the area becomes executable; do not use this directory as evidence of implementation by itself.
