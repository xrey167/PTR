# Tests for model/configs

This directory holds tests, fixtures, validation cases, or executable checks owned by `model/configs`. Add real tests here as the area becomes executable; do not use this directory as evidence of implementation by itself.
