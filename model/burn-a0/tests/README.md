# PTR Burn A0 tests

Forward-shape and autodiff smoke tests use the Burn NdArray backend so CI does not require a GPU. CUDA/CubeCL performance and numerical behavior are separate model-framework evaluation work.
