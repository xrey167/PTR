[package]
name = "ptr-burn-a0"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
publish = false
license = "MIT OR Apache-2.0"

[dependencies]
bytemuck = "=1.23.1"
burn = { version = "=0.18.0", default-features = false, features = ["std", "ndarray", "autodiff"] }

[workspace]