# Reviewed dependency source patches

The JSON manifest records immutable upstream inputs, archive SHA256, local changes and file hashes. Package identities are retained. These sources replace dependencies/APIs, not audit metadata. All upstream license and notice files are preserved. Review these forks when upgrading; root and model workspace patches are deliberately separate.
