# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "raft-proto"
version = "0.7.0"
authors = ["The TiKV Project Developers"]
build = false
description = "Protocol definitions for the rust language implementation of the Raft algorithm."
homepage = "https://github.com/pingcap/raft-rs"
documentation = "https://docs.rs/raft-proto"
readme = "README.md"
keywords = [
    "raft",
    "distributed-systems",
    "ha",
]
categories = [
    "algorithms",
    "database-implementations",
]
license = "Apache-2.0"
repository = "https://github.com/pingcap/raft-rs"

# PTR: the supported adapter is prost-only; generated source is pinned and reviewed.
[dependencies.prost]
version = "0.11.9"

[features]
default = ["prost-codec"]
prost-codec = []
