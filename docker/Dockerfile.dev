FROM rust:1.85-bookworm
RUN apt-get update && apt-get install -y --no-install-recommends python3 python3-pip git curl ca-certificates build-essential && rm -rf /var/lib/apt/lists/*
RUN python3 -m pip install --break-system-packages uv
WORKDIR /workspace
