# Tests for training/configs

This directory holds tests, fixtures, validation cases, or executable checks owned by `training/configs`. Add real tests here as the area becomes executable; do not use this directory as evidence of implementation by itself.
