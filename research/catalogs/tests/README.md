# Tests for research/catalogs

The architecture catalog is validated by `scripts/check_architecture_catalog.py` and the corresponding script tests.

The catalog must remain machine-readable, use unique IDs, reference real owning crates, and avoid silently marking open design questions as selected implementation.
