version = 1
channel = "alpha"
artifacts = ["ptrd", "ptrctl", "ptr-worker", "ptr-bench"]
include = ["Cargo.lock", "training/uv.lock", "CHANGELOG.md", "LICENSE-MIT", "LICENSE-APACHE"]
sbom = "cyclonedx-1.5"
provenance = "github-actions-sigstore-attestation"
checksums = ["sha256"]
signing = "sigstore-via-github-attestation"
