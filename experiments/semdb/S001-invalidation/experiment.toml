version = 1
id = "S001"
status = "planned"
hypothesis = "Does dependency-aware invalidation avoid unnecessary recomputation while preserving exact semantics?"
metrics = "affected-set precision/recall; recompute time"
baseline = "global recomputation"
falsification = "Reject if semantic results diverge or invalidation offers no locality advantage."
hardware_profile = "hardware/default.toml"
entrypoint = ""
seeds = [17, 29, 43, 71, 101]
results_dir = "results"
required_artifacts = ["run.json", "metrics.json"]
