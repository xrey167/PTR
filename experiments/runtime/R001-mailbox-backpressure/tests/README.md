# Tests for experiments/runtime/R001-mailbox-backpressure

This directory holds tests, fixtures, validation cases, or executable checks owned by `experiments/runtime/R001-mailbox-backpressure`. Add real tests here as the area becomes executable; do not use this directory as evidence of implementation by itself.
