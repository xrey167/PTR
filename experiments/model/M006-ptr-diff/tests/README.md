# Tests for experiments/model/M006-ptr-diff

This directory holds tests, fixtures, validation cases, or executable checks owned by `experiments/model/M006-ptr-diff`. Add real tests here as the area becomes executable; do not use this directory as evidence of implementation by itself.
