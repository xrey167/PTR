# Tests for experiments/model/M002-typed-attention

This directory holds tests, fixtures, validation cases, or executable checks owned by `experiments/model/M002-typed-attention`. Add real tests here as the area becomes executable; do not use this directory as evidence of implementation by itself.
