version = 1
id = "M007"
status = "planned"
hypothesis = "Does interleaved structured model/runtime execution reduce latency and invalid tool calls?"
metrics = "task time; tool errors; resumability"
baseline = "text-only tool-call loop"
falsification = "Reject if streaming structured events do not improve latency/validity."
hardware_profile = "hardware/default.toml"
entrypoint = ""
seeds = [17, 29, 43, 71, 101]
results_dir = "results"
required_artifacts = ["run.json", "metrics.json"]
