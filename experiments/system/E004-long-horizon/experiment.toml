version = 1
id = "E004"
status = "planned"
hypothesis = "Test state fidelity over long multi-turn projects and revisions."
metrics = "semantic preservation; stale-state errors; context tokens"
baseline = "full-context + strong RAG project memory"
falsification = "Reject if long-horizon fidelity/efficiency does not exceed baselines."
hardware_profile = "hardware/default.toml"
entrypoint = ""
seeds = [17, 29, 43, 71, 101]
results_dir = "results"
required_artifacts = ["run.json", "metrics.json"]
