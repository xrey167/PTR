# Tests for benchmarks/rag-baselines

This directory holds tests, fixtures, validation cases, or executable checks owned by `benchmarks/rag-baselines`. Add real tests here as the area becomes executable; do not use this directory as evidence of implementation by itself.
