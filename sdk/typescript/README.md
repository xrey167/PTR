# @ptr-ai/client

Minimal dependency-free JavaScript runtime client with TypeScript declarations.

Current contract:

- `GET /health`
- `POST /v1/requests`

The SDK is **not released** and the server API is not yet frozen. It exists so server/API work can be tested against a concrete client contract rather than inventing request shapes ad hoc.
