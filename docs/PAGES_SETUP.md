# Documentation Site / GitHub Pages

`docs-site/` is a dependency-free static landing page that links to the versioned architecture/status documents in the repository.

GitHub Pages is intentionally not enabled automatically because repository-administration settings are outside the current connector. To publish it, configure Pages for a GitHub Actions deployment or a dedicated branch, then keep the site links pointing to versioned repository documents rather than duplicating architecture text.
