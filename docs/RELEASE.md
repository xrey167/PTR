# Release Process

PTR has no stable release yet. The first release target is an alpha only after current CI, lifecycle tests and an end-to-end baseline run are reproducible.

Release artifacts should include binaries, source, lockfiles, SHA-256 checksums, an SBOM and release notes. Signing is planned before any production-oriented release.
