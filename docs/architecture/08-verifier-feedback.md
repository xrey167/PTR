# Verifier and Feedback Fabric

Verification is a fabric, not a single LLM judge. Deterministic and executable verification outrank learned judges. Candidate populations can be generated, clustered, verified, critiqued, patched or rewritten, then selected. Successful and failed trajectories feed training.

Hard runtime invariants are never subject to GEPA or RL optimization.
