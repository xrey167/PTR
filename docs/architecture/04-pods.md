# Pod Architecture

Pods are cognitive primitives with semantic type contracts. The model learns `Predict<FeatureVector, Distribution<Direction>>`, not a hard-coded endpoint string. Pods may be native, process-isolated, remote, GPU-backed or environment-backed. A Pod manifest declares accepted/produced types, capabilities, effects and protocol version.
