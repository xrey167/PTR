# Tests for integrations/ledger

This directory holds tests, fixtures, validation cases, or executable checks owned by `integrations/ledger`. Add real tests here as the area becomes executable; do not use this directory as evidence of implementation by itself.
