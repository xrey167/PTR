# Tests for integrations/editor

This directory holds validation fixtures and executable checks for optional editor/developer integrations. Editor tooling must never be required to build, test, run or release PTR.
