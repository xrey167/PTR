name: one-shot-raft-rs-sync
on:
  push:
    paths:
      - '.github/workflows/one-shot-raft-rs-sync.yml'
permissions:
  contents: write
jobs:
  verify:
    if: github.ref == 'refs/heads/main' && github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - uses: actions/setup-python@v7
        with:
          python-version: '3.13'
      - run: sudo apt-get update && sudo apt-get install -y protobuf-compiler
      - run: cargo +stable fmt --all
      - run: cargo +stable generate-lockfile
      - run: cargo +stable test -p ptr-ledger --features raft-rs-backend --test raft_rs --locked
      - run: python scripts/update_component_docs.py --write
      - name: Commit verified lock/docs
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Synchronize verified raft-rs integration"
          git pull --rebase origin main
          git push origin HEAD:main
# rerun with protoc installed
